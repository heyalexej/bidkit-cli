# Developer API namespace

eBay platform/account-management APIs for the developer's own keyset.

```bash
bidkit developer analytics get-rate-limits --api-context sell --api-name inventory
bidkit developer key-management get-signing-key                    # Finances signing material
```

These are read-only over your application's own analytics and signing keys.
