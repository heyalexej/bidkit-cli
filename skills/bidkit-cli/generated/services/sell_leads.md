# Classified Leads API

- **Service key:** `sell_leads`
- **CLI:** `bidkit sell leads`
- **Version:** v1.0.0
- **Base path:** `/sell/leads/v1`  ·  **Subdomain:** `apiz`
- **Auth scheme:** `Bearer`  ·  **Requires signature:** False
- **Source spec:** `sell_leads_v1_oas3.json`
- **Operations:** 2

Inspect any operation's full metadata or schema without a network call:

```bash
bidkit api describe sell_leads.OPERATION_ID
bidkit api schema sell_leads.OPERATION_ID request
```

| Operation key | Method | Path | Risk | Summary |
|---|---|---|---|---|
| `sell_leads.getAllClassifiedLeads` | GET | `/classified_lead` | read | This method retrieves leads for all of the seller's active classified ad listings. Optiona |
| `sell_leads.getClassifiedLeadsByItemId` | GET | `/classified_lead/{itemId}` | read | This method returns a seller account's leads generated by the specified classified ad and  |

Command path prefix: `bidkit sell leads <operation>`.

## Examples

```bash
# sell_leads.getAllClassifiedLeads
bidkit sell leads get-all-classified-leads --format json
# sell_leads.getClassifiedLeadsByItemId
bidkit sell leads get-classified-leads-by-item-id ITEM-ID --format json
```

More (including execute examples with the required safety flags): `bidkit api examples <service>.<operationId>`.
